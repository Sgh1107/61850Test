# IEC61850 Wrapper 测试记录清单

> 使用方式：每完成一项测试，可将对应的 `- [ ]` 改为 `- [x]`，表示该项已通过。

---

## 一、基础连接测试

- [√] 连接服务端成功
- [√] 连接状态查询成功
- [√] KeepAlive 成功
- [√] 主动断开连接成功

### 备注
- 服务端 IP：192.168.31.68
- 服务端端口：102
- 测试时间：2026-05-15
- 结果说明：服务端61850版本为1.6

---

## 二、读取功能测试

### 1. float 读取
- [√] float 点读取成功
- [√] 返回值类型正确
- [√] 返回值内容符合预期

测试点：`simpleIOGenericIO/GGIO1.AnIn1.mag.f`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --read-ref "simpleIOGenericIO/GGIO1.AnIn1.mag.f" --read-type float`

### 2. bool 读取
- [√] bool 点读取成功
- [√] 返回值类型正确
- [√] 返回值内容符合预期

测试点：`simpleIOGenericIO/GGIO1.SPCSO1.stVal`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --read-ref "simpleIOGenericIO/GGIO1.SPCSO1.stVal" --read-type bool`

### 3. int32 / uint32 读取
- [√] int32 或 uint32 点读取成功
- [√] 返回值类型正确
- [√] 返回值内容符合预期

测试点：`LD0/SomeIntPoint.stVal`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --read-ref "LD0/SomeIntPoint.stVal" --read-type int32`

### 4. timestamp / bitstring 读取（可选）
- [√] timestamp 点读取成功
- [√] bitstring 点读取成功

测试点：
`LD0/LLN0.Mod.t`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --read-ref "LD0/LLN0.Mod.t" --read-type timestamp`

---

## 三、写入功能测试

> 注意：写入测试前请确认服务端点位允许写入。

### 1. bool 写入
- [×] bool 写入成功
- [√] 写入后读回验证成功

测试点：`GenericIO/GGIO1.SPCSO1.stVal写入1`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --write-ref "simpleIOGenericIO/GGIO1.SPCSO1.stVal" --write-type bool --write-value 1 --skip-read`  
❌ 测试报错：`access denied`  
🔴 原因：该点不允许通过普通写服务直接写入，应使用控制服务 Operate

### 2. int32 / uint32 写入
- [ ] int32 或 uint32 写入成功
- [ ] 写入后读回验证成功

测试点：`LD0/SomeUintPoint.stVal写入10`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --write-ref "LD0/SomeUintPoint.stVal" --write-type uint32 --write-value 10 --skip-read`

### 3. float / double 写入
- [ ] float 或 double 写入成功
- [ ] 写入后读回验证成功

测试点：`LD0/SomeFloatPoint.mag.f写入12.5`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --write-ref "LD0/SomeFloatPoint.mag.f" --write-type float --write-value 12.5 --skip-read`

---

## 四、遥控功能测试

> 注意：遥控前请确认服务端模型支持控制对象。

### 1. Direct Operate
- [√] direct 模式遥控成功
- [√] 遥控结果符合预期

测试点：`simpleIOGenericIO/GGIO1.SPCSO1置1`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --control-ref "simpleIOGenericIO/GGIO1.SPCSO1" --control-mode direct --control-type bool --control-value 1 --skip-read`

### 2. SBO Operate
- [ ] SBO 模式遥控成功
- [ ] 遥控结果符合预期

测试点：`LD0/CSWI1.Pos`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --control-ref "LD0/CSWI1.Pos" --control-mode sbo --control-type bool --control-value 1 --skip-read`

---

## 五、报告功能测试

> 注意：报告测试前请确认服务端已配置对应 RCB。

- [√] EnableReport 成功
- [√] 收到报告回调
- [√] 报告项数量正确
- [√] DisableReport 成功

测试点 / RCB：  `simpleIOGenericIO/LLN0.RP.EventsIndexed01`  
测试命令：  
`iec61850_wrapper_test.exe --server 192.168.31.68 --report-ref "simpleIOGenericIO/LLN0.RP.EventsIndexed01" --skip-read`

---

## 六、异常场景测试

- [ ] 错误 IP 连接失败符合预期
- [ ] 错误点号读取失败符合预期
- [ ] 错误类型读取失败符合预期
- [ ] 未连接直接读写返回 not connected

---

## 七、最终测试结论

- [√] wrapper 基础连接功能通过
- [√] wrapper 读取功能通过
- [ ] wrapper 写入功能通过
- [√] wrapper 遥控功能通过
- [√] wrapper 报告功能通过
- [ ] wrapper 异常处理功能通过
